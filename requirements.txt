# Recomputing the tables and figures from the released results (CPU only)
numpy>=1.24
matplotlib>=3.7
scipy>=1.10

# Additionally required to preprocess, train and attack from scratch
torch>=2.0
wfdb>=4.1
scikit-learn>=1.3
